# AI Framework Configuration
app_env: "dev"
debug: true

# SDK Settings
llm_provider: "mock"
llm_model: "mock-gpt-4"

# App Settings
database_url: "sqlite:///./test.db"
log_level: "DEBUG"
json_logs: true
